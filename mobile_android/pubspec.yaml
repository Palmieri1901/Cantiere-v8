name: portomare_lavori
description: App dipendenti GEB Nautica / Portomare - registrazione lavori in cantiere.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  uuid: ^4.4.0
  qr_flutter: ^4.1.0
  mobile_scanner: ^5.1.1
  connectivity_plus: ^6.0.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
